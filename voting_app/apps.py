# Here are my imports
from django.apps import AppConfig

# Below is a class 'VotingAppConfig'
class VotingAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'voting_app'
